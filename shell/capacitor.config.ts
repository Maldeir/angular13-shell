import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chrisshelltest.app',
  appName: 'chrisshelltest',
  webDir: 'dist/shell',
  bundledWebRuntime: false
};

export default config;
